<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.exposure">

    <!-- Internet Permission Granted -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/expo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".Activity.Business.ListAllProductsActivity"></activity>
        <activity android:name=".Activity.Business.ListAllBusinessesActivity" />
        <activity
            android:name=".Activity.Organizer.AddEventActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Activity.Organizer.ListAllEventsActivity" />
        <activity android:name=".Activity.Visitor.VisitorMainActivity" />
        <activity android:name=".Activity.Business.BusinessMainActivity" />
        <activity android:name=".Activity.Organizer.OrganizerMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>